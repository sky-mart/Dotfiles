office_tower:
  show_battery: false
  has_bluetooth: false

home_tower:
  show_battery: false
  has_bluetooth: true
  default_bluetooth_device: "mac-addr-or-name"

work_laptop:
  show_battery: false
  has_bluetooth: true
  default_bluetooth_device: "mac-addr-or-name"
